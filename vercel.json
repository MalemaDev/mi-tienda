{
  "buildCommand": "cd client && rm -rf node_modules package-lock.json && npm install --legacy-peer-deps --force && node ./node_modules/vite/bin/vite.js build",
  "outputDirectory": "client/dist",
  "installCommand": "cd server && npm install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/server/api/index.js"
    }
  ]
}